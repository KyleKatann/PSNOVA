# Tools

Generation and migration utilities live here and are not part of the GitHub Pages public tree.

Current notebook-based table makers are retained as legacy tools while structured datasets and deterministic generators are introduced under the repository root.

Do not place generated public HTML here. Published output belongs under `docs/`.
